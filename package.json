{
  "name": "deadlock_web",
  "version": "1.0.0",
  "type": "module",
  "description": "A web application to analyze team performance in Deadlock matches by comparing players' recent statistics.",
  "main": "index.js",
  "scripts": {
    "build-css": "tailwindcss -i ./src/input.css -o ./styles.css --watch",
    "build-css-prod": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo 'Build already complete - files are in public directory'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vercel/analytics": "^1.5.0",
    "chart.js": "^4.5.0",
    "node-fetch": "^3.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
